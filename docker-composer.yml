version: '3'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "5000:5000"
    depends_on:
      - frontend

  frontend:
    build:
      context: ./frontend
    # The frontend service is not exposed directly as its content is served by the backend.
    # ports:
    #   - "3000:3000"
